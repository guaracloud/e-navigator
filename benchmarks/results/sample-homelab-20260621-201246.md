# Homelab Live Validation Summary: 20260621-201246

Curated summary for raw local artifacts under
`benchmarks/results/20260621-201246/`.

## Scope

- Context: `staging`
- Namespace: `e-navigator-bench`
- Release: `e-navigator-bench`
- Required image: `ghcr.io/e-navigator/e-navigator:sha-8ab271c`
- Configured image: `ghcr.io/e-navigator/e-navigator:sha-5c417c0`
- Image substitution: yes
- Pull secret: namespace GHCR pull secret configured
- Prometheus HTTP: enabled
- ServiceMonitor: enabled
- Cleanup: not run

The required image was not used because current runtime configuration needed a
fresh pushed image that included the Prometheus sink fix. This run validates
only `sha-5c417c0` on the `staging` homelab context.

## Proven

- DaemonSet rolled out `2/2` Ready on `homelab-01` and `homelab-02`.
- Runtime logs contained 5,598 parsed JSON signal lines, including
  `source.aya_exec`, `source.aya_network`, `source.host_resource`,
  network/resource generators, trace correlation, dependency graph, and runtime
  security findings.
- The Prometheus HTTP endpoint returned HTTP 200 with 72 metric lines.
- Late Prometheus API capture reported both E-Navigator targets `up`.
- Late `scrape_samples_scraped` values were `134` and `368`.
- Late Prometheus series query returned 567 E-Navigator job series.
- `network_connection_open_count` was queryable from Prometheus and included
  Kubernetes/container labels, including the controlled workload pod.
- A bounded label check found no raw server address/port, cgroup path, process
  command, container ID, pod UID, DNS question name, or secret-like terms in the
  captured E-Navigator `/metrics` output.

## Not Proven

- Reduced-privilege eBPF operation. The pod ran as root with `SYS_ADMIN`,
  `NoNewPrivs: 1`, and `Seccomp: 0`.
- Live eBPF DNS packet capture from `source.aya_dns`.
- OTLP, trace backend, external profile backend, pprof, or production collector compatibility.
- Production soak, cardinality baseline, or reduced-overhead comparison.
- Replacement readiness for external flow agent, Alloy, trace backend, Prometheus, or external profile backend.

## Capability Snapshot

The live `/proc/1/status` capability mask was `000001c401283004`, decoded as:

`DAC_READ_SEARCH`, `NET_ADMIN`, `NET_RAW`, `SYS_PTRACE`, `SYS_ADMIN`,
`SYS_RESOURCE`, `SYSLOG`, `PERFMON`, `BPF`, and `CHECKPOINT_RESTORE`.

## Final State

The release and workload were left running because cleanup was explicitly
disabled for this run.
