apiVersion: v1
kind: Service
metadata:
  name: e-navigator
  namespace: e-navigator-system
  labels:
    app.kubernetes.io/name: e-navigator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: e-navigator
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
