# Claims Matrix

| Area | Implemented | Synthetic-only | Non-privileged proven | Privileged-proven | Deferred |
| --- | --- | --- | --- | --- | --- |
| Static pipeline runtime | yes | no | cargo tests and synthetic CLI | no | runtime plugin loading |
| JSON stdout envelopes | yes | no | cargo tests, Docker smoke | no | storage/UI |
| Process exec source | yes | no | userspace config and decode tests | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, `20260620-180718-failed-inconclusive-validation`, and `20260621-170331-live-validation` observed `source.aya_exec` records from live Kubernetes nodes; the 20260621 run proved controlled workload Kubernetes/container attribution for exec records | reduced privilege hardening |
| TCP network source | yes | no | raw decode tests | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, `20260620-180718-failed-inconclusive-validation`, and `20260621-170331-live-validation` observed `source.aya_network` records from live Kubernetes nodes; the 20260621 run proved controlled workload Kubernetes/container attribution for network and derived records | full TCP state, packet accounting |
| Runtime DNS capture | partial registered source boundary | yes, DNS envelopes and fixtures | schema/generator/smoke tests plus bounded DNS packet parser/raw decode tests for `source.aya_dns` | no | live eBPF DNS packet capture and workload DNS proof |
| Host resource source | yes | no | procfs/sysfs/cgroup parser tests and Docker synthetic fixtures | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, and `20260620-180718-failed-inconclusive-validation` observed `source.host_resource` node, process, and cgroup records from mounted host paths | warning detail expansion, longer host accuracy baseline |
| Container attribution processor | yes | no | cgroup, container runtime, Kubernetes cache, label bound, and cross-signal enrichment tests | guarded homelab run `20260621-170331-live-validation` proved controlled workload namespace, pod name, pod UID, container name, node name, bounded labels, and containerd ID on exec, network, dependency, metric, and trace-derived records | complete attribution for every host process |
| Dependency graph | yes | no | generator tests and runner fan-out tests | no | persisted service map |
| Trace foundation | yes | partly | schema, generator, formatter, Docker smoke | no | full OTLP trace export, trace storage, UI, critical path analysis |
| Request/protocol foundation | yes | fixture-backed | traceparent and HTTP fixture tests | no | live HTTP/gRPC parsing, routes, retries, app errors |
| CPU profiling source | yes, explicit opt-in source | no | raw decode, profile normalization, generator tests | homelab `aya-cpu-profile` canaries observed `source.aya_cpu_profile` sample envelopes and bounded raw IP stack frames, including `20260620-180718-failed-inconclusive-validation` and `20260621-170331-live-validation`; the 20260621 run observed attributed samples for other live workloads, but not the controlled CPU workload | function symbolization, controlled workload attribution, pprof, Pyroscope, OTLP profiles, flamegraph UI, storage |
| Guara Beyla L4 compatibility projection | partial | no | golden schema tests plus generator and sink formatter tests | no | live byte-accurate Aya flow cache, active timeout flushing, cross-node runtime dedupe, Prometheus endpoint runtime |
| Guara Tempo service graph compatibility | partial | partly | trace formatter tests for `service.name`, `k8s.namespace.name`, `k8s.pod.name`, and `k8s.deployment.name` | no | OTLP trace transport, Tempo ingestion proof, live HTTP/gRPC/database spans, context propagation |
| Guara Pyroscope CPU identity | partial | no | profile formatter tests for `process_cpu:cpu:nanoseconds:cpu:nanoseconds` and Guara labels | no | Pyroscope write transport, OTLP profile transport, symbolization/demangling runtime proof |
| Prometheus HTTP sink | yes, opt-in `sink.prometheus_http` | no | registry/config tests and local HTTP tests for `/metrics`, `/healthz`, `/readyz`, bounded metric state, and secret-like label filtering | no | live ServiceMonitor/Prometheus scrape and active target proof |
| OTLP HTTP sink boundary | partial, opt-in `sink.otlp_http` over internal records | no | registry/config tests plus fake-collector tests for timeout, retry, bounded queue, batch sizing, and signal-family switches | no | full OTLP protobuf metrics/traces/profiles, Tempo/Pyroscope/collector compatibility proof |
| Exporter infrastructure | partial | no | registered sink tests for `sink.json_stdout`, `sink.prometheus_http`, and `sink.otlp_http`; sink-layer fake-collector tests for batching, timeout config, retry, bounded queue, headers, and drop counters; `20260619-210812-extended` and `20260620-180718-failed-inconclusive-validation` confirmed only `sink.json_stdout` was registered in that older release | no | full OTLP protobuf metrics/traces/profiles, production collector compatibility proof |
| Benchmark evidence harness | yes | no | local Criterion compile and smoke runs for deterministic parser, decode, generator, formatter, and queue fixtures | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, `20260620-180718-failed-inconclusive-validation`, and `20260621-170331-live-validation` recorded raw DaemonSet, pod, log, event, workload, capability, resource, and metrics artifacts | committed raw results, live overhead baselines, homelab runtime comparison |
| Supply-chain and secret hygiene checks | yes | no | `cargo deny`, `cargo audit`, `cargo machete`, release SBOM/signature workflow, and tracked-file secret-pattern guard | no | container vulnerability policy gates, full secret-scanner integration |
| Kubernetes packaging | yes | no | `helm lint`, `helm template`, `kubeconform -strict -summary`, packaged config guards that verify embedded TOML lists every registered module as enabled or disabled and validates through the CLI parser, and chart Service/ServiceMonitor exposure guards | guarded homelab runs `20260619-205746` and `20260621-170331-live-validation` rolled out the DaemonSet Ready on both homelab nodes; the 20260621 run used the required public image without a pull secret and restored default mode after the profile canary | reduced-privilege eBPF hardening, production rollout proof |

`Privileged-proven` must remain `no` unless the exact privileged smoke command or guarded homelab canary is run on a real Linux host or Kubernetes cluster and the observed output is recorded.
