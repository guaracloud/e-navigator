# Claims Matrix

| Area | Implemented | Synthetic-only | Non-privileged proven | Privileged-proven | Deferred |
| --- | --- | --- | --- | --- | --- |
| Static pipeline runtime | yes | no | cargo tests and synthetic CLI | no | runtime plugin loading |
| JSON stdout envelopes | yes | no | cargo tests, Docker smoke | no | storage/UI |
| Process exec source | yes | no | userspace config and decode tests | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, `20260620-180718-failed-inconclusive-validation`, `20260621-170331-live-validation`, `20260621-194840`, and `20260621-201246` observed `source.aya_exec` records from live Kubernetes nodes; 2026-06-21 runs proved controlled workload Kubernetes/container attribution for captured exec or derived records | reduced privilege hardening |
| TCP network source | yes | no | raw decode tests | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, `20260620-180718-failed-inconclusive-validation`, `20260621-170331-live-validation`, `20260621-194840`, and `20260621-201246` observed `source.aya_network` records from live Kubernetes nodes; 2026-06-21 runs proved controlled workload Kubernetes/container attribution for captured network and derived records | full TCP state, packet accounting |
| Runtime DNS capture | partial live source | no | schema/generator/smoke tests plus bounded DNS packet parser/raw decode tests for `source.aya_dns`; `20260622-013602-dns-msg-live` and `20260622-213109-dns-connected-udp-live-r2` passed full `scripts/quality.sh` before rollout; `94e808c` adds DNS-local connected UDP peer tracking plus `write`/`read` tracepoint coverage | `20260622-012214-dns-live` deployed pushed image `sha-7e746f5` and proved initial live `source.aya_dns` output plus `generator.dns_metrics` from homelab DNS traffic, but controlled BusyBox workload DNS was not attributed; `20260622-013602-dns-msg-live` then deployed pushed image `sha-10b81e6` with widened `sendmsg`/`recvmsg` tracepoints, kept both homelab pods Ready, observed DNS source and metric records with Kubernetes/container attribution for CoreDNS and Pi-hole activity, but the controlled BusyBox client-to-CoreDNS workload did not appear in DNS attribution; `20260622-213109-dns-connected-udp-live-r2` deployed pushed image `sha-94e808c` as Helm revision 45, ran connected-UDP Python DNS clients pinned to both homelab nodes, and observed the warmed `homelab-02` client pod `e-nav-dns-connected-udp-r3-20260622-213109-homelab-02-5thbm` with 120 attributed `dns_query` records and 120 attributed `dns_response` records against `10.43.0.10:53`; the `homelab-01` controlled client completed but did not produce matching controlled-client DNS records in the final structured pass, and dropped DNS perf events were reported; `20260623-051700-dns-seccomp-live` deployed pushed image `sha-beec11d` under chart `RuntimeDefault` seccomp as Helm revision 104, both E-Navigator pods reported `Seccomp: 2` and `NoNewPrivs: 1`, and the observed `homelab-02` connected-UDP Python `os.write`/`os.read` workload produced 148 attributed `dns_query` records, 148 attributed `dns_response` records, 296 attributed `dns_counter_metric` records, and 148 attributed `dns_latency_metric` records against `10.43.0.10:53` after attribution warmup | symmetric controlled-client DNS attribution across both nodes, lossless DNS event capture, full DNS response coverage across nodes, DNS replacement readiness |
| Host resource source | yes | no | procfs/sysfs/cgroup parser tests and Docker synthetic fixtures | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, and `20260620-180718-failed-inconclusive-validation` observed `source.host_resource` node, process, and cgroup records from mounted host paths | warning detail expansion, longer host accuracy baseline |
| Container attribution processor | yes | no | cgroup, container runtime, Kubernetes cache, label bound, and cross-signal enrichment tests | guarded homelab runs `20260621-170331-live-validation` and `20260621-194840` proved controlled workload namespace, pod name, pod UID, container name, node name, bounded labels, and containerd ID on captured exec, network, dependency, metric, and trace-derived records; `20260621-203358-profile-live` proved the same Kubernetes/container identity fields on controlled CPU profile samples and profiling sessions | complete attribution for every host process |
| Dependency graph | yes | no | generator tests and runner fan-out tests | guarded homelab run `20260621-233103-generator-resource-security-live` observed a live `generator.dependency_graph` `dependency_edge` from `source.aya_network` with Kubernetes/container attribution for an `infisical-system` workload and destination `10.43.0.10:53` | persisted service map, complete service topology |
| Runtime security generator | yes | no | exact-match generator tests, golden signal coverage, and synthetic CLI output | guarded homelab run `20260621-234159-runtime-security-live` on `staging`/`e-navigator-bench` observed 209 live `runtime_security_finding` records from `generator.runtime_security` across both homelab nodes: 135 `runtime.network_tool_exec`, 59 `network.kubernetes_api_from_workload`, and 15 `runtime.shell_in_container`; controlled workload Kubernetes/container attribution was present for both pinned workload pods on exec-derived findings, while some network-derived findings still had no attribution | broad policy engine, complete attribution for every finding, production alert routing |
| Trace foundation | yes | partly | schema, generator, formatter, Docker smoke, and fake-collector OTLP protobuf trace request test | `20260622-160350-otlp-trace-protobuf-live` proved namespace-local OpenTelemetry Collector acceptance of synthetic trace/request spans from pushed image `sha-c00a7d5` | live application HTTP/gRPC trace extraction, trace storage, UI, critical path analysis |
| Request/protocol foundation | partial live HTTP source | fixture-backed plus opt-in live source | traceparent and HTTP fixture tests, including bounded origin-form and absolute-form HTTP `url.path` extraction without query/fragment values, bounded `http.request.id` extraction from request ID headers, bounded `server.address`/`server.port` extraction from HTTP Host authorities and absolute-form request targets, and request-span propagation/dedupe tests; `cfd7ea8` adds disabled-by-default `source.aya_http` outbound client request capture for connected TCP socket `write`/`sendto`/`sendmsg` paths with bounded raw request copying and local decode/registry/packaged-config guards; `fb9a6d1` adds disabled-by-default `sys_enter_writev` HTTP request capture with a structural guard; `7ac7ef2` removes verifier-panic-prone dynamic request slicing from split-iovec BPF request copying and keeps the split-iovec guard; `30c2026` keeps three fixed HTTP iovec slots but bounds each slot to 96 bytes to stay verifier-loadable on the homelab kernel | `20260622-231600-http-live` deployed pushed image `sha-cfd7ea8` digest `sha256:c2c850cffcc1209bebfce2e9915728718b4ab2e04a6873f9b25c59dc884e968c` as Helm revision 52 in `staging/e-navigator-bench` with `source.aya_http` and `generator.request_correlation` enabled, kept both homelab pods Ready, observed live `protocol_request_observation` from `source.aya_http` plus `request_span_observation` from `generator.request_correlation` for real cluster traffic including Kubernetes-attributed CoreDNS `GET /health`; the Python controlled client completed 50 requests and the BusyBox `nc` controlled client completed 80 requests, but neither controlled client produced matching protocol/request-span records; `20260622-234023-http-writev-live` then deployed pushed image `sha-fb9a6d1` linux/amd64 digest `sha256:2c984944dee476bfdb27ecaa473277152a4f7b304a0ed99d24b867a90dbba751` as Helm revision 54, ran a Python client/server pinned to `homelab-02`, completed 120 cleartext HTTP requests through `os.writev`, and observed 120 matching `protocol_request_observation` plus 120 matching `request_span_observation` records for `/proof/http-writev-20260622-234023`; 101 of each included Kubernetes namespace, pod, and container attribution for client pod `e-nav-http-writev-20260622-234023-client-msdw5`, while the first 19 matching pairs had no Kubernetes fields yet; `20260623-030630-http-iovec-live-r9` deployed pushed image `sha-7ac7ef2` linux/amd64 digest `sha256:7967acb8ca974c6e0fbdd578c33d1229bfb04b8112ebbc7c546eccaea3b99818` as Helm revision 72, kept both homelab pods Ready with zero restarts, found none of the previous BPF verifier failure markers in startup logs, ran split `writev` Python workloads pinned to `homelab-02`, observed 80 protocol records plus 80 request-span records for `/proof/http-iovec-r9-20260623-030630` with 80 unique request IDs, then observed 80 protocol records plus 80 request-span records for `/proof/http-iovec-r9b-20260623-030630` with 80 unique request IDs and Kubernetes namespace `e-navigator-bench`, pod `http-iovec-r9b-dptfg`, and container `workload` on every measured proof record; `20260623-033542-http-three-iovec-bounded-live` deployed pushed image `sha-30c2026` linux/amd64 digest `sha256:2d17c1e7aeccc59c3ac73ef7b32684b9215b8b0db4f138376b0f0f32ef24778c` as Helm revision 94, kept both homelab pods Ready, ran a Python workload pinned to `homelab-02` with request-line, Host, and request-ID headers split across three `os.writev` iovecs of 24, 27, and 71 bytes, observed 80 measured `protocol_request_observation` plus 80 measured `request_span_observation` records for `/proof/iovec3-033542` with 80 unique `i3-proof-*` request IDs and Kubernetes namespace `e-navigator-bench`, pod `http-iovec3-033542-gsbxx`, and container `workload` on every measured proof record, then deleted the temporary proof/control Jobs and rolled back to revision 95/rollback-to-93 with baseline digest `sha256:90b571bf89ac36c1432a503ad9b9add7abd7604579533c1912201568db1d5bfc`; `20260623-045606-http-seccomp-live` deployed pushed image `sha-643ea37` under chart `RuntimeDefault` seccomp as Helm revision 102, both E-Navigator pods reported `Seccomp: 2` and `NoNewPrivs: 1`, and a controlled three-iovec Python workload on `homelab-02` produced 80 measured `protocol_request_observation` plus 80 measured `request_span_observation` records for `/proof/iovec3-seccomp-045606` with 80 unique `i3-seccomp-proof-*` request IDs and Kubernetes namespace, pod, and container attribution on every measured proof record | symmetric node coverage, TLS/plaintext limitations beyond observed cleartext bytes, gRPC framing, inbound server-side parsing, status-code extraction, route templates, retries, app errors, more than three iovec slots, and chunks larger than the configured 96-byte slot size |
| CPU profiling source | yes, explicit opt-in source | no | raw decode, profile normalization, generator tests | homelab `aya-cpu-profile` canaries observed `source.aya_cpu_profile` sample envelopes and bounded raw IP stack frames, including `20260620-180718-failed-inconclusive-validation` and `20260621-170331-live-validation`; `20260621-203358-profile-live` emitted 10,284 final `source.aya_cpu_profile` records and mid-run evidence included 274 controlled workload `profile_sample_observation` records plus 274 attributed `generator.profiling` sessions for pod `e-navigator-bench-profile-20260621-203358-7qx72`; `20260623-065356-live-profile-otlp-aya` deployed pushed image `sha-6037089` with `source.aya_cpu_profile`, `generator.profiling`, and profile-only `sink.otlp_http` enabled, captured 33 controlled workload `profile_sample_observation` records plus 33 controlled workload `profiling_session_observation` records for pod `live-profile-otlp-cpu-20260623-065356-spk75`, and exported live profile protobuf accepted by a namespace-local OpenTelemetry Collector; `20260623-043123-profile-seccomp-live` deployed pushed image `sha-6c739f8` under chart `RuntimeDefault` seccomp, kept both pods Ready with `Seccomp: 2`, and observed live `source.aya_cpu_profile` plus `generator.profiling` output, but both controlled CPU workload Jobs completed without matching profile records | controlled workload profile attribution under RuntimeDefault seccomp, function symbolization, pprof, Pyroscope write transport, flamegraph UI, storage |
| Guara Beyla L4 compatibility projection | partial | no | golden schema tests plus generator and sink formatter tests; `sha-762561f` added live read/write byte counters and close-derived `network_flow_summary` generation with local tests; `sha-622e1aa` adds an eBPF source guard that keeps Linux `-EINPROGRESS` nonblocking connect returns out of the failure-only path; `sha-86b3fce` adds socket send/recv byte accounting for `sendto`, `sendmsg`, `recvfrom`, and `recvmsg` with a structural guard in `scripts/quality.sh` | guarded homelab run `20260622-111022-guara-flow-live` deployed GHCR image `sha-762561f` digest `sha256:d520fd8b7bd0a4042c31513034d43f716b75407a888b47468f19ca3504629a5a` as Helm revision 41, kept both DaemonSet pods Ready, and observed 234 byte-bearing `network_connection_close` records plus 53 live `network_flow_summary` records, but the two-node BusyBox workload completed 360 controlled HTTP requests without Kubernetes attribution on its byte-bearing close records, so controlled workload flow summaries were not proven; follow-up `20260622-111448-guara-flow-python-client-live` kept Python client pods alive after 160 controlled socket reads, but the captured server-IP records were `EINPROGRESS` connection failures and still produced 0 controlled byte-close records, 0 controlled `network_flow_summary` rows, and 0 `beyla_network_flow_bytes_total` signals; follow-up `20260622-122803-guara-einprogress-live` deployed GHCR image `sha-622e1aa` digest `sha256:90b571bf89ac36c1432a503ad9b9add7abd7604579533c1912201568db1d5bfc` as Helm revision 42 and proved the observed homelab-02 nonblocking Python client target emitted 120 opens, 120 closes, 0 failures, and 0 errno 115 failures plus matching direct `/metrics` aggregate counters, but it still produced 0 byte-bearing controlled closes, 0 controlled `network_flow_summary` rows, 0 `beyla_network_flow_bytes_total` signals, no Kubernetes attribution on the Python client records, and no stdout records for the successful homelab-01 target; follow-up `20260622-220427-socket-bytes-live` deployed GHCR image `sha-86b3fce` digest `sha256:72acf600c86be7b9a2a0c4ca8ae905e065232e26125e1cdf575f515c53668a48` as Helm revision 48 and proved the observed homelab-02 controlled Python client target emitted 120 byte-bearing `network_connection_close` records with `bytes_sent=243` and `bytes_received=1372`, while direct `/metrics` stayed healthy and exposed aggregate controlled network counters at 120; the same run still produced 0 Kubernetes-attributed controlled closes, 0 controlled `network_flow_summary` rows, 0 `beyla_network_flow_bytes_total` signals, and no symmetric controlled-client stdout capture across both nodes; Prometheus API query was not run because the hard boundary kept live actions in `e-navigator-bench` and no Prometheus server service exists there; follow-up `20260622-192821-new-container-attribution-live` deployed GHCR image `sha-dd67a3b` digest `sha256:5082072c88fb2b525a5d860484cd1bf16a4c2d2870af2d31ff9bdb08819a638d` as Helm revision 50 and proved the observed homelab-02 controlled Python client pod emitted 34 byte-bearing Kubernetes-attributed `network_connection_close` records plus 34 controlled `network_flow_summary` records, while direct homelab-02 `/metrics` exposed Kubernetes-attributed aggregate client counters at 1,574 opens, destination observations, and duration samples; the same run still produced 0 `beyla_network_flow_bytes_total` JSON signals and 0 direct `/metrics` Beyla lines because the temporary workload namespace is not in Guara `proj-*` scope | positive direct and Prometheus runtime export of `beyla_network_flow_bytes_total`, Guara `proj-*` scoped controlled workload proof, symmetric controlled-client capture across both nodes, broader controlled attribution coverage beyond the observed homelab-02 client, active timeout flushing, cross-node runtime dedupe |
| Guara Tempo service graph compatibility | partial | partly | trace formatter tests for `service.name`, `k8s.namespace.name`, `k8s.pod.name`, and `k8s.deployment.name`; fake-collector test proves valid trace records are encoded as OTLP protobuf `ExportTraceServiceRequest` payloads with `application/x-protobuf` | `20260622-160350-otlp-trace-protobuf-live` proved pushed image `sha-c00a7d5` can deliver synthetic trace/request spans as OTLP protobuf to a namespace-local OpenTelemetry Collector, including `service.name = synthetic-api`, trace ID `4bf92f3577b34da6a3ce929d0e0e4736`, and `http.request.method = GET`; this is collector acceptance proof, not Tempo storage/query proof | Tempo storage/query proof, live HTTP/gRPC/database spans, context propagation |
| Guara Pyroscope CPU identity | partial | no | profile formatter tests for `process_cpu:cpu:nanoseconds:cpu:nanoseconds` and Guara labels; OTLP profile protobuf fake-collector test for native profile records; `sha-796b980` aligns development-status profile protobuf encoding with the OpenTelemetry Collector `0.130.0` profile schema | homelab run `20260622-204027-otlp-profile-protobuf-live` used pushed image `sha-796b980` digest `sha256:61d9761205ba29da86de0acea4cff102d6a6d8278eca41408e05b25a1772a908` against a namespace-local OpenTelemetry Collector `0.130.0` profile pipeline on `staging`/`e-navigator-bench`; the one-shot Job completed, Collector debug logs decoded `ResourceProfiles` with synthetic stack frame names plus `Location indices: [1 2]` and `[1 2 3 4]`, and no `sink write failed`, HTTP 400/404, or wire-type failure markers were found; `20260623-065356-live-profile-otlp-aya` then proved live Aya/eBPF CPU profile samples and generated profiling sessions exported as development-status OTLP profile protobuf accepted by a namespace-local OpenTelemetry Collector from pushed image `sha-6037089`, with 1,874 decoded `ResourceProfiles` and zero precise sink/collector failure markers | Pyroscope write transport, symbolization/demangling runtime proof, exact controlled-workload pod identity in Collector debug output |
| Prometheus HTTP sink | yes, opt-in `sink.prometheus_http` | no | registry/config tests and local HTTP tests for `/metrics`, `/healthz`, `/readyz`, bounded metric state, and secret-like label filtering | guarded homelab run `20260621-201246` deployed image `sha-5c417c0` in `staging`/`e-navigator-bench`, exposed `/healthz`, `/readyz`, and non-empty `/metrics` over port 9090, created a ServiceMonitor, and late Prometheus API capture reported both E-Navigator targets `up`, `scrape_samples_scraped` values `134` and `368`, `567` E-Navigator job series, and queryable `network_connection_open_count` samples with Kubernetes/container labels; `20260622-001716-published-image-live` deployed pushed GHCR image `sha-d3167e3`, restored the baseline config after Alloy failure-boundary testing, returned `/healthz`=`ok`, `/readyz`=`ready`, `53` `/metrics` lines, and homelab Prometheus API results with two `up` targets, `25` `network_connection_open_count` series, and `323` E-Navigator job series; `20260622-004011-current-head-live` rolled current pushed image `sha-c89f345`, returned `/healthz`=`ok`, `/readyz`=`ready`, `348` `/metrics` lines, two Prometheus `up` targets, `486` E-Navigator job series, and controlled workload Prometheus series for `network_connection_open_count{k8s_namespace_name="e-navigator-bench"}` and `network_connection_failure_count{k8s_namespace_name="e-navigator-bench"}` | longer soak, cardinality baseline, and correctness under production load |
| OTLP HTTP sink boundary | partial, opt-in sink.otlp_http with metric, trace, and development-status profile protobuf support | no | registry/config tests plus fake-collector tests for timeout, retry, bounded queue, batch sizing, signal-family switches, valid metric records encoded as OTLP protobuf ExportMetricsServiceRequest payloads with application/x-protobuf, valid trace records encoded as OTLP protobuf ExportTraceServiceRequest payloads with application/x-protobuf, and profile records encoded as OTLP protobuf ExportProfilesServiceRequest payloads with application/x-protobuf | 20260622-135450-otlp-metric-protobuf-live proved namespace-local OpenTelemetry Collector 0.130.0 acceptance of 45 OTLP protobuf metrics from pushed image sha-e7016b5; 20260622-160350-otlp-trace-protobuf-live proved namespace-local OpenTelemetry Collector 0.130.0 acceptance of two OTLP protobuf trace spans from pushed image sha-c00a7d5; 20260622-204027-otlp-profile-protobuf-live proved namespace-local OpenTelemetry Collector 0.130.0 acceptance of synthetic OTLP protobuf profile records from pushed image sha-796b980 through the feature-gated /v1development/profiles route; 20260623-065356-live-profile-otlp-aya proved live Aya/eBPF profile observations and profiling sessions exported as OTLP profile protobuf to the same namespace-local Collector profile route from pushed image sha-6037089; older fake-collector and failure-boundary homelab runs prove internal JSON delivery and non-fatal sink-failure behavior only | Tempo/Pyroscope storage proof, broad production collector compatibility, exact controlled-workload pod identity in Collector debug output, longer profile-mode soak |
| Exporter infrastructure | partial | no | registered sink tests for `sink.json_stdout`, `sink.prometheus_http`, and `sink.otlp_http`; sink-layer fake-collector tests for batching, timeout config, retry, bounded queue, headers, drop counters, OTLP protobuf metric request encoding, OTLP protobuf trace request encoding, and development-status OTLP protobuf profile request encoding; runner tests prove sink write failures are logged and non-fatal while source failures still propagate | `20260621-201246` proved live Prometheus HTTP exposure; `20260622-135450-otlp-metric-protobuf-live` proved live namespace-local OpenTelemetry Collector acceptance for OTLP protobuf metric export; `20260622-160350-otlp-trace-protobuf-live` proved live namespace-local OpenTelemetry Collector acceptance for OTLP protobuf trace export; `20260622-204027-otlp-profile-protobuf-live` proved live namespace-local OpenTelemetry Collector acceptance for development-status OTLP protobuf synthetic profile export; `20260623-065356-live-profile-otlp-aya` proved live Aya/eBPF profile export through `sink.otlp_http` to a namespace-local Collector profile route; older fake-collector and HTTP 500/400 runs prove delivery boundaries and non-fatal sink failures | Tempo/Pyroscope storage proof, broad production collector ingestion proof, longer live profile export soak |
| Resource and privilege baseline | partial evidence only | no | Kubernetes resource requests/limits render through Helm and local chart tests; chart guard verifies the default DaemonSet container security context renders `seccompProfile.type: RuntimeDefault` | `20260621-221235-baseline-resource-live` recorded a collection-only homelab snapshot on `sha-5c417c0`: DaemonSet `2/2` Ready, 10 `kubectl top` samples per E-Navigator pod (`34m`-`42m`/`47Mi` on `homelab-01`, `12m`/`36Mi` on `homelab-02`), Prometheus cAdvisor CPU and memory series for both pods, resource requests and limits, decoded capabilities, `NoNewPrivs: 1`, and `Seccomp: 0`; `20260622-001716-published-image-live` recorded 10 samples on pushed image `sha-d3167e3` (`38m`-`42m`/`43Mi` on `homelab-01`, `12m`-`17m`/`35Mi`-`36Mi` on `homelab-02`) and decoded the same `CapEff` mask with `NoNewPrivs: 1` and `Seccomp: 0`; `20260622-004011-current-head-live` recorded 10 samples on current pushed image `sha-c89f345` (`36m`-`39m`/`51Mi` on `homelab-01`, `12m`-`17m`/`36Mi`-`37Mi` on `homelab-02`) and decoded UID/GID `0`, the same `CapEff` mask, `NoNewPrivs: 1`, and `Seccomp: 0`; `20260622-013602-dns-msg-live` recorded 5 samples on pushed image `sha-10b81e6` with DNS enabled (`44m`-`47m`/`68Mi` on `homelab-01`, `15m`-`18m`/`59Mi` on `homelab-02`) and the same capability/security posture; `20260623-041434-runtime-default-seccomp-live` rolled the existing homelab DaemonSet values through the local chart default as Helm revision `98`, kept both pods Ready, proved kernel-applied `Seccomp: 2` plus `NoNewPrivs: 1` in both pods, observed live `source.aya_network` logs and 264 direct `/metrics` lines, then rolled back to revision `97`; `20260623-043123-profile-seccomp-live` rolled `source.aya_cpu_profile` and `generator.profiling` under `Seccomp: 2` and observed live profile source/generator output with no verifier or seccomp failures, but no controlled workload profile attribution; `20260623-045606-http-seccomp-live` rolled `source.aya_http` and `generator.request_correlation` under `Seccomp: 2`, proved controlled three-iovec HTTP capture plus request-span generation, and recorded direct homelab-02 `/metrics` controlled workload counters; `20260623-051700-dns-seccomp-live` rolled `source.aya_dns` and `generator.dns_metrics` under `Seccomp: 2`, proved controlled homelab-02 DNS capture plus DNS metric generation after attribution warmup, and recorded direct homelab-02 health/readiness/metrics output; no reduced-overhead or reduced-privilege claim is made because there was no equivalent baseline comparison and `CAP_SYS_ADMIN` plus UID 0 remained | equivalent baseline-agent comparison, longer soak, reduced-capability eBPF proof, non-root eBPF proof, controlled CPU workload attribution under RuntimeDefault |
| Required image availability | yes, pull/run and DaemonSet check | no | release workflow publishes tagged images and local install docs identify `sha-8ab271c` as the required benchmark image | `20260621-221944-required-image-live` proved `ghcr.io/guaracloud/e-navigator:sha-8ab271c` is pullable in `staging`/`e-navigator-bench` with pull secret `ghcr-e-navigator-pull`, starts, prints CLI help, exits `0`, and resolves to digest `sha256:249ad67fa8578ade9ecc1279bcf52a52ae6038a342b7c68844ebfd7a38d4e34e`; `20260621-222508-required-image-daemonset-live` then rolled the same image digest as the live Helm DaemonSet on both homelab nodes with an older compatible config, completed workload `e-nav-required-workload-222834`, observed live `source.aya_network`, `generator.network_metrics`, and `generator.trace_correlation` JSON stdout records, and restored the release to `sha-5c417c0` with Prometheus Service/ServiceMonitor back in place | feature parity with newer images, live exec/DNS/profile/host-resource output on `sha-8ab271c`, Prometheus/OTLP export on `sha-8ab271c`, signature/SBOM verification in-cluster |
| Benchmark evidence harness | yes | no | local Criterion compile and smoke runs for deterministic parser, decode, generator, formatter, and queue fixtures | guarded homelab runs `20260619-205746`, `20260619-210812-extended`, `20260620-180718-failed-inconclusive-validation`, `20260621-170331-live-validation`, `20260621-193917`, `20260621-194840`, `20260621-201246`, `20260621-205344-otlp-live`, `20260621-214450-sink-failure-live`, `20260621-220029-guara-compat-live`, `20260621-221235-baseline-resource-live`, `20260621-221944-required-image-live`, `20260621-222508-required-image-daemonset-live`, `20260621-224414-alloy-otlp-boundary-live`, `20260621-233103-generator-resource-security-live`, `20260621-234159-runtime-security-live`, `20260622-001716-published-image-live`, `20260622-004011-current-head-live`, `20260622-013602-dns-msg-live`, `20260622-111022-guara-flow-live`, `20260622-111448-guara-flow-python-client-live`, `20260622-122803-guara-einprogress-live`, `20260622-160350-otlp-trace-protobuf-live`, `20260622-135450-otlp-metric-protobuf-live`, `20260622-204027-otlp-profile-protobuf-live`, `20260622-213109-dns-connected-udp-live-r2`, `20260622-234023-http-writev-live`, `20260623-030630-http-iovec-live-r9`, `20260623-065356-live-profile-otlp-aya`, `20260623-041434-runtime-default-seccomp-live`, `20260623-043123-profile-seccomp-live`, and `20260623-045606-http-seccomp-live` recorded raw DaemonSet, pod, log, event, workload, capability, resource, metrics, collector, image-pull/import, and restore artifacts; `20260621-201246` records image substitution from required `sha-8ab271c` to pushed `sha-5c417c0`, pull-secret-backed GHCR rollout, and late Prometheus scrape/query evidence; `20260621-214450-sink-failure-live` records local image import to node containerd after GHCR push scope failure; `20260621-220029-guara-compat-live` records negative Guara compatibility runtime boundary evidence and restore correction; `20260621-221235-baseline-resource-live` records collection-only resource/capability evidence with no cleanup or Helm mutation; `20260621-221944-required-image-live` records required-image pull/run evidence with no Helm mutation; `20260621-222508-required-image-daemonset-live` records required-image DaemonSet runtime evidence and leaves completed evidence resources in the namespace; `20260621-224414-alloy-otlp-boundary-live` records real Alloy HTTP 400 rejection, local image import, non-fatal sink behavior in current code, and restore; `20260621-233103-generator-resource-security-live` records current collection-only dependency graph, Prometheus resource/network metric queries, resource samples, capability posture, and explicit runtime-security/host-resource stdout non-observations; `20260621-234159-runtime-security-live` records pinned two-node workload proof for live `generator.runtime_security`, Prometheus scrape/query evidence, host-resource stdout, and capability posture; `20260622-001716-published-image-live` records pushed GHCR image `sha-d3167e3`, successful GitHub CI/image workflow, homelab rollout, real Alloy HTTP 400 non-fatal sink behavior, restored Prometheus scrape proof, resource samples, and capability posture; `20260622-004011-current-head-live` records current pushed image `sha-c89f345`, full local quality gate before rollout, homelab rollout, controlled workload completion, JSON stdout family counts, Prometheus controlled workload network attribution, resource samples, and capability posture; `20260622-013602-dns-msg-live` records pushed image `sha-10b81e6`, successful GitHub CI/image workflow, homelab rollout, live DNS source/generator output, controlled DNS workload non-attribution, health/metrics exposure, resource samples, and capability posture; `20260622-111022-guara-flow-live` and `20260622-111448-guara-flow-python-client-live` record pushed image `sha-762561f`, live byte-counter and ambient flow-summary proof, and controlled flow attribution/Beyla negative evidence; `20260622-122803-guara-einprogress-live` records pushed image `sha-622e1aa`, nonblocking connect failure-path proof for the observed homelab-02 target, direct health/metrics evidence, workload cleanup, and remaining byte-flow/Beyla negative evidence; `20260622-160350-otlp-trace-protobuf-live` records namespace-local OpenTelemetry Collector acceptance of OTLP trace protobuf; `20260622-135450-otlp-metric-protobuf-live` records namespace-local OpenTelemetry Collector acceptance of OTLP metric protobuf; `20260622-204027-otlp-profile-protobuf-live` records namespace-local OpenTelemetry Collector acceptance of synthetic OTLP profile protobuf; `20260622-213109-dns-connected-udp-live-r2` records pushed image `sha-94e808c`, connected-UDP DNS client proof with Kubernetes attribution for the observed homelab-02 Python workload, cleanup, rollback, and remaining cross-node/lossless-capture negative evidence; `20260622-234023-http-writev-live` records pushed image `sha-fb9a6d1`, controlled HTTP writev client proof with Kubernetes attribution for the observed homelab-02 Python workload, cleanup, rollback, and remaining symmetric-node/TLS/gRPC/inbound/multi-iovec negative evidence; `20260623-030630-http-iovec-live-r9` records pushed image `sha-7ac7ef2`, split `writev` HTTP request proof with Kubernetes attribution for the paced observed homelab-02 Python workload, cleanup, rollback, and remaining symmetric-node/TLS/gRPC/inbound/beyond-two-iovec negative evidence; `20260623-065356-live-profile-otlp-aya` records pushed image `sha-6037089`, live Aya CPU profile export through `sink.otlp_http`, namespace-local Collector profile protobuf acceptance, cleanup, rollback, and remaining Collector-workload-identity/Pyroscope/storage/symbolization non-claims; `20260623-041434-runtime-default-seccomp-live` records local chart RuntimeDefault seccomp proof, live homelab rollout as revision `98`, kernel `Seccomp: 2` evidence on both pods, live network logs/metrics under that profile, workload cleanup, rollback to revision `97`, and remaining non-root/capability/all-source-mode non-claims; `20260623-043123-profile-seccomp-live` records CPU profile source/generator output under `Seccomp: 2` plus controlled workload non-attribution; `20260623-045606-http-seccomp-live` records bounded three-iovec HTTP request/span proof under `Seccomp: 2`, controlled workload metrics, cleanup, and rollback to revision `101` | committed raw results, live overhead baselines, homelab runtime comparison |
| Supply-chain and secret hygiene checks | yes | no | `cargo deny`, `cargo audit`, `cargo machete`, release SBOM/signature workflow, and tracked-file secret-pattern guard | no | container vulnerability policy gates, full secret-scanner integration |
| Kubernetes packaging | yes | no | `helm lint`, `helm template`, `kubeconform -strict -summary`, packaged config guards that verify embedded TOML lists every registered module as enabled or disabled and validates through the CLI parser, chart Service/ServiceMonitor exposure guards, a ConfigMap checksum rollout guard, and a default `RuntimeDefault` seccomp guard | guarded homelab runs `20260619-205746`, `20260621-170331-live-validation`, `20260621-193917`, `20260621-194840`, and `20260621-201246` rolled out the DaemonSet Ready on both homelab nodes; `20260621-201246` ran image `sha-5c417c0` with a namespace GHCR pull secret, `privileged: false`, `allowPrivilegeEscalation: false`, `NoNewPrivs: 1`, and `Seccomp: 0`, but still ran as root with `SYS_ADMIN`; `20260623-041434-runtime-default-seccomp-live` rolled the same baseline image and release values through the local chart default, proved both pods Ready with `Seccomp: 2`, observed live network output and metrics, then restored the prior live revision; `20260623-045606-http-seccomp-live` rolled the current pushed image through the same chart `RuntimeDefault` seccomp default and proved the bounded HTTP source/request generator path still worked before rollback; `20260623-051700-dns-seccomp-live` rolled the current pushed image through the same chart `RuntimeDefault` seccomp default and proved the observed homelab-02 connected-UDP DNS source/generator path still worked before rollback | reduced-capability eBPF hardening, non-root eBPF hardening, production rollout proof |

`Privileged-proven` must remain `no` unless the exact privileged smoke command or guarded homelab canary is run on a real Linux host or Kubernetes cluster and the observed output is recorded.

DNS follow-up note: `20260623-005331-dns-homelab01-negative-live` deployed
pushed image `sha-635819e` to `staging`/`e-navigator-bench` with
`source.aya_dns` and `generator.dns_metrics` enabled, verified controlled
connected and explicit-sockaddr Python DNS clients on `homelab-01` received
CoreDNS responses from `10.43.0.10:53`, but found zero matching E-Navigator
records by pod name, pod IP, container ID, command, or exact query names. It is
negative evidence for `homelab-01` controlled-client DNS capture and does not
change the existing symmetric/lossless/full-coverage deferred claims.

DNS diagnostic follow-up note:
`20260623-045808-dns-bpf-drop-diagnostics-not-proven` rolled three pushed
diagnostic images only to `staging`/`e-navigator-bench`. The first two images
failed BPF loading in `source.aya_dns` with verifier output pointing at
`tracepoint_recvfrom_exit` and `try_tracepoint_dns_recvfrom_exit`; the third
failed BPF loading in `source.aya_network` at `tracepoint_read_exit`. Commit
`e3bc6f2` reverted the diagnostics, GitHub `CI` run `28004511957` and
`publish-images` run `28004511962` succeeded for the revert, and the release was
restored to the previous baseline digest. This does not prove DNS diagnostic
events, the root cause of the missing `homelab-01` controlled-client records,
or any wider DNS readiness claim.

HTTP three-iovec follow-up note:
`20260623-030344-http-three-iovec-live` first showed that unbounded three-slot
copy expansion was verifier-hostile on the homelab kernel. The bounded
follow-up `20260623-033542-http-three-iovec-bounded-live` then proved live
three-slot capture for the exact 96-byte-per-slot shape described above.

CPU profile seccomp follow-up note:
`20260623-043123-profile-seccomp-live` deployed pushed image `sha-6c739f8` to
`staging`/`e-navigator-bench` with `source.aya_cpu_profile` and
`generator.profiling` enabled under chart `RuntimeDefault` seccomp. The
DaemonSet stayed `2/2` Ready, both pods reported `Seccomp: 2` and
`NoNewPrivs: 1`, and E-Navigator emitted live profile samples and profiling
sessions with zero precise verifier, module, seccomp, permission, sink, or
panic failure markers. Two controlled BusyBox CPU workload Jobs completed on
`homelab-02`, including an eight-process `yes` workload, but neither pod name
nor `command="yes"` appeared in captured profile records. This is partial
evidence for CPU profile source compatibility with `RuntimeDefault`, not
controlled workload attribution or broader profiling readiness.

HTTP seccomp follow-up note:
`20260623-045606-http-seccomp-live` deployed pushed image `sha-643ea37` to
`staging`/`e-navigator-bench` with `source.aya_http`,
`source.aya_network`, `generator.request_correlation`,
`generator.network_metrics`, `sink.json_stdout`, and `sink.prometheus_http`
enabled under chart `RuntimeDefault` seccomp. Both E-Navigator pods stayed
Ready and reported `Seccomp: 2` plus `NoNewPrivs: 1`. A controlled Python
workload on `homelab-02` completed 80 three-iovec proof HTTP requests with
zero errors and produced 80 `protocol_request_observation` records plus 80
`request_span_observation` records for `/proof/iovec3-seccomp-045606`, all
with Kubernetes namespace, pod, and container attribution. Direct homelab-02
`/metrics` exposed Kubernetes-attributed controlled workload network counters.
This proves the bounded three-slot HTTP path under RuntimeDefault for the
observed homelab-02 workload; it does not prove DNS source modes beyond the
exact recorded DNS run, symmetric HTTP coverage, TLS, gRPC, inbound parsing,
or reduced capabilities.

DNS seccomp follow-up note:
`20260623-051700-dns-seccomp-live` deployed pushed image `sha-beec11d` to
`staging`/`e-navigator-bench` with `source.aya_dns`, `source.aya_network`,
`generator.dns_metrics`, `generator.network_metrics`, `sink.json_stdout`, and
`sink.prometheus_http` enabled under chart `RuntimeDefault` seccomp. Both
E-Navigator pods stayed Ready and reported `Seccomp: 2` plus `NoNewPrivs: 1`.
The final controlled Python workload on `homelab-02` completed 120 connected
UDP DNS `os.write`/`select`/`os.read` proof requests with zero errors and
produced 148 attributed `dns_query` records, 148 attributed `dns_response`
records, 296 attributed `dns_counter_metric` records, and 148 attributed
`dns_latency_metric` records for pod `dns-seccomp-051700-r4-86x9c` after
attribution warmup. Two earlier workload shapes in the same run were not
controlled-proof positive: `socket.send`/`recv` completed but produced no
matching controlled-client DNS records, and a nonblocking `os.read` variant
failed with `Errno 11`. This proves the observed homelab-02 connected-UDP
`write`/`read` DNS path under RuntimeDefault; it does not prove symmetric
node coverage, lossless capture, full syscall/path coverage, DNS replacement
readiness, or reduced capabilities.
